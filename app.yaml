runtime: nodejs12
vpc_access_connector:
        name: projects/portfolio-285502/locations/us-east1/connectors/portfolio-vpc
handlers:
# Serve all static files with url ending with a file extension
- url: /(.*\..+)$
  static_files: build/\1
  upload: build/(.*\..+)$
# Catch all handler to index.html
- url: /.*
  static_files: build/index.html
  upload: build/index.html
  
env_variables:
  CLOUD_SQL_USERNAME: postgres
  CLOUD_SQL_PASSWORD: Nhannon03231996!?><
  CLOUD_SQL_DATABASE: PortfolioDB
  CLOUD_SQL_CONNECTION: portfolio-285502:us-east1:portfolio-db